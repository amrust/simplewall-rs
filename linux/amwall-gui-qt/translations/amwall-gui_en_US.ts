<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE TS>
<!--
  amwall-gui — English (United States) translation source.

  This file is the seed for Qt's translation infrastructure. It's
  empty on purpose: the source language is en_US, so every tr() call
  already shows the right string with no translator. The file exists
  so:

    1. lupdate scans every tr() in the codebase and populates this
       file with <message> elements at build time, giving future
       contributors a complete list of strings to translate.
    2. CMake's qt_add_translations(TS_FILES ...) treats it as the
       template — copying it to amwall-gui_de.ts (or whichever
       locale) and translating produces a real translation.

  After editing source code that adds/changes tr() strings, run from
  this dir to refresh the catalog:

      lupdate -recursive ../src -ts amwall-gui_en_US.ts

  Or just rebuild with cmake — qt_add_translations triggers lupdate
  automatically.
-->
<TS version="2.1" language="en_US" sourcelanguage="en_US">
</TS>
